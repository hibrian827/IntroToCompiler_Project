int x=0,y=1;
x++; y--; x+=1; y-=1;        /* +=, -=는 정의 안 했으므로 '+' '-' '='가 따로 찍힐 것 */
if (x==y && x!=0 || y>=1 && y<=10) { x = y->next; }  /* ->, <=, >=, ==, !=, &&, || */
x = (y < 3) ? y++ : --y;     /* '?' ':'는 정의 안 했으므로 각각 개별 문자(unknown rule이면 skip) */
x = x % 2; x = x * 3 / 4;    /* %, *, / */
x = x & y;                   /* & (단항/비트) */
a .. b;                      /* 공백 포함 '..' */
(a) [b] {c} . , ! * / % + - < > & ; =
